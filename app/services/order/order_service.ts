import env from '#start/env'
import Order from '#models/order'
import type { OrderRequest } from '#controllers/interface/order.interface'
import { HttpContext } from '@adonisjs/core/http'

class OrderService {
    async add(payload: OrderRequest) {
        
    }
}
